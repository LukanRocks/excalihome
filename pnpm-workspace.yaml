packages:
  - 'web'
  - 'backend'

# Supply-chain hardening
strictDepBuilds: true
minimumReleaseAge: 2880 # minutes = 48h cooldown on newly published versions
trustPolicy: no-downgrade
trustPolicyIgnoreAfter: 129600 # minutes = 90d; versions older than this skip trust checks (minimumReleaseAge covers the fresh end)

# Force patched lodash-es (GHSA-r5fr-rjxr-66jc, GHSA-f23m-r3pf-42rh, GHSA-xxjr-mmjv-4gpg)
# !TODO: Review in future package updates if this override still required
overrides:
  'lodash-es@<4.18.0': '>=4.18.0'

allowBuilds:
  '@tailwindcss/oxide': true
  better-sqlite3: true
  esbuild: true
  sharp: true
